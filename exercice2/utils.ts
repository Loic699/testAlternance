import { User } from './model/user';
export const displayLoadingIcon = function(display: boolean) {
    // Code affichant ou masquant l'icone 
    // Il ne faut pas compléter cette fonction
  }

  // Affichage des données
  export const displayData = function(data: User[]) {
    // Code affichant les données
    // Il ne faut pas compléter cette fonction
  }

  // Affichage d'un message d'erreur
  export const displayError = function(statusCode: number) {
    // Code affichant un message d'erreur en fonction du status code
    // Il ne faut pas compléter cette fonction
  }